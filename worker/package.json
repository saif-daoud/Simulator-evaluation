{
  "name": "cbt-simulator-evaluation-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "node local_server.mjs",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir dist",
    "test": "node --test tests/*.test.js",
    "test:e2e": "node tests/e2e.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.54.0"
  }
}
